---
name: Release
on:
  push:
    branches:
      - main

env:
  # renovate: datasource=npm depName=semantic-release
  SEMANTIC_RELEASE_VERSION: 25.0.9
  # https://github.com/semantic-release/release-notes-generator/issues/992
  # renovate: datasource=npm depName=conventional-changelog-conventionalcommits
  CONVENTIONAL_COMMITS_PRESET_VERSION: 9.3.1

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  release:
    name: Release
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .nvmrc
      - name: Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: >-
          npx --package=semantic-release@${{ env.SEMANTIC_RELEASE_VERSION }}
          --package=conventional-changelog-conventionalcommits@${{ env.CONVENTIONAL_COMMITS_PRESET_VERSION }} --
          semantic-release
